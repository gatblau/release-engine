package catalog

// Canonical catalogue names used across template engine and tests.
// Keep these in sync with definitions/*.yaml `name` fields.
const (
	K8sAppName   = "k8s-app"
	VMAppName    = "vm-app"
	DataProcName = "data-proc"
)
